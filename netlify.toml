[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — required for React Router and Firebase Google redirect return URLs.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
